# QA run — 2026-07-08 run 1 (Slice 0 / QA gate 0, partial)

**Scope:** gate-scoped run for Task 0.3 (compose stack + test harness), not a full test cycle — only the CHK items relevant to the slice-0 startup contract were executed. All items run manually against the live compose stack (`docker compose down -v && docker compose up --build`, Windows 11 / Docker Desktop). Master checklist: [../test-checklist.md](../test-checklist.md); cycle summary in [../qa-review-log.md](../qa-review-log.md) (2026-07-08 gate-0 entry).

| ID | Check | Refs | Pri | Result | Notes |
|---|---|---|---|---|---|
| CHK-AUTH-19 | No credentials, SMTP secrets, or hard-coded user passwords anywhere in the repository | BR-A15; DoD-8 | P0 | Pass | **Carve-out:** `${POSTGRES_PASSWORD:-dev-only-not-a-secret}` in `docker-compose.yml` is the declared bounded exception (architecture.md §2), labeled dev-only inline; `.env.example` holds placeholder names only; `.gitignore` excludes `.env`/`.env.*`. Signed off by qa-reviewer 2026-07-08. |
| CHK-OPS-01 | Clean checkout: `docker compose up --build` starts frontend, backend, DB; app usable with nothing else installed | BR-O01; DoD-7; R2 | P0 | Pass | All 3 containers healthy; SPA shell 200 at `/` and `/board`; `/api/health` → `{"status":"ok"}`; Mailpit UI 200 on :8025. Healthcheck uses `127.0.0.1` (alpine resolves `localhost` to `::1`; server binds IPv4). Run on Windows only this cycle — macOS/Linux pending. |
| CHK-OPS-02 | Fresh init: DB contains schema + migration metadata only | BR-P10; DoD-9 | P0 | Pass | psql after `down -v`: users/teams/epics/tickets/comments/email_verification_tokens all 0 rows; `_prisma_migrations` 1 row; exactly 7 tables. |
| CHK-OPS-03 | Repeatable init: `down -v` then `up --build` again succeeds with same fresh state | BR-P09 | P0 | Pass | Full cycle executed twice this session; identical fresh state both times. |
| CHK-OPS-04 | Default startup path loads no sample/seed data (incl. no default admin in migrations) | BR-P10; R8; DoD-8/9 | P0 | Pass | grep of `prisma/migrations` finds no INSERT/COPY; container CMD is `prisma migrate deploy && node dist/server.js` only. **Carve-out note:** compose dev-only DB password default per CHK-AUTH-19. |
| CHK-OPS-10 | Three-tier separation evident: SPA ↔ HTTP API ↔ server-based RDBMS container | BR-O02, O03 | P2 | Pass | Three compose services; SPA↔API over `/api/*` JSON only; dedicated postgres:16 container; separate `frontend/`/`backend/` source trees, multi-stage Dockerfile. |

**Automated tests this run:** `cd backend && npm test` → vitest, 2 files / 4 tests passed (health contract; SPA-fallback vs JSON-404 split).
